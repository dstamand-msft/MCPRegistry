Folder: StoredProcedures

Purpose: Hold `CREATE PROCEDURE` object scripts for the `MCPRegistryDatabase` project. Organize by schema or feature. Each file should contain a single stored procedure and the file name should match the object name (e.g., `dbo.GetCustomerOrders.sql`).

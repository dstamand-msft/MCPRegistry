Folder: Data

Purpose: Hold idempotent seed and static data scripts for the `MCPRegistryDatabase` project. Use `IF EXISTS`/`MERGE` patterns where possible and group by domain. Keep one logical dataset per file.
